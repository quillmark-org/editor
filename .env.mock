# Mock mode configuration
# AUTH_MOCK=true enables Auth.js mock credentials provider (auto-login as mock user)
# DATABASE_DRIVER=pglite uses in-memory PostgreSQL (set separately in .env.dev or here)

AUTH_MOCK=true
DISABLE_MOBILE=false
